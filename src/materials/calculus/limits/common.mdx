# Limits of Trigonometric Functions

<Formula>
Given the constants $a$, and $b$:

$$
\begin{align*}
\text{1. }&\lim\limits_{x\rightarrow 0}\frac{\sin{ax}}{bx}=\lim\limits_{x\rightarrow 0}\frac{ax}{\sin{bx}}=\frac{a}{b}\\
\text{2. }&\lim\limits_{x\rightarrow 0}\frac{\tan{ax}}{bx}=\lim\limits_{x\rightarrow 0}\frac{ax}{\tan{bx}}=\frac{a}{b}\\
\text{3. }&\lim\limits_{x\rightarrow 0}\frac{\sin{ax}}{\tan{bx}}=\lim\limits_{x\rightarrow 0}\frac{\tan{ax}}{\sin{bx}}=\frac{a}{b}\\
\text{4. }&\lim\limits_{x\rightarrow 0}\frac{\sin{ax}}{\sin{bx}}=\lim\limits_{x\rightarrow 0}\frac{\tan{ax}}{\tan{bx}}=\frac{a}{b}\\ \end{align*}
$$

</Formula>

Although the above limits are easy to remember, you may still wonder how are they possible.

Based on the above formulas, you can tell that the following limit is correct.

$$
\lim\limits_{x\rightarrow 0}\frac{\sin{x}}{x}=1
$$

However, we will try to **prove** it, and there are many ways to prove this limit. This proof will require us to use the **Squeeze Theorem**.

===

# Proof of a Trigonometric Limit

<Theorem>
**Squeeze Theorem**

Suppose that $f(x) ≤ g(x) ≤ h(x)$ for all $x$ in an open interval containing $a$, except possibly at $a$, and $\lim\limits_{x\rightarrow a}f(x)= \lim\limits_{x\rightarrow a} h(x)=L$. So, $\lim\limits_{x\rightarrow a} g(x)=L$.

</Theorem>

![Squeeze Theorem](/materials/squeeze_theorem_proof.png "Squeeze Theorem")

First, we will refer to $\angle BOC$ as $x$ in radian, and the circle's radius as $r$. Note that $BC$ and $DA$ are perpendicular to $OA$ and $OC$. Based on this, we can see that:

- $OB$'s length = $OA$'s length = $r$
- $BC$'s length = $r \sin{x}$
- $OC$'s length = $r \cos{x}$
- $AD$'s length = $r \tan{x}$

Next, we will extract three different shapes; the triangle $BOC$, the sector $BOA$, and the triangle $DOA$. Just by looking at the visualization, we can tell that:
$$ \text{Area }BOC \lt \text{Area }BOA \lt \text{DOA} $$

Before continuing, we must find each of the region's area:

- $\text{Triangle } BOC = \frac{1}{2} r^2 \sin{x} \cos{x}$
- $\text{Sector } BOA = \frac{1}{2}xr^2$
- $\text{Triangle } DOA = \frac{1}{2} r^2 \tan{x}$

Now that we know each of the area, we can plug the formula in:

$$
\begin{align*}
\text{Area }BOC &<  \text{Area }BOA < \text{Area }DOA\\
\frac{1}{2} r^2 \sin{x} \cos{x} &< \frac{1}{2} x r^2 < \frac{1}{2}r^2 \tan{x} \\
\sin{x} \cos{x} &< x < \tan{x} \\
\cos{x} &< \frac{x}{\sin{x}} < \frac{1}{\cos{x}} \\
\end{align*}
$$

Next, we will take the limit of all terms:

$$
\begin{align*}
\lim\limits_{x\rightarrow 0} \cos{x} &< \lim\limits_{x\rightarrow 0} \frac{1}{\sin{x}} < \lim\limits_{x\rightarrow 0} \cos{x} \\
1 &< \lim\limits_{x\rightarrow 0} \frac{x}{\sin{x}} < 1 \\
\end{align*}
$$

Based on **Squeeze Theorem**, we can conclude that $\lim\limits_{x\rightarrow 0} \frac{x}{\sin{x}} = 1$. Although this was not what we wanted to prove initially, we can just play with limit's properties:

$$
\begin{align*}
\lim\limits_{x\rightarrow 0} \frac{x}{\sin{x}} &= 1\\
\frac{1}{\lim\limits_{x\rightarrow 0} \frac{x}{\sin{x}}} &= \frac{1}{1}\\
\lim\limits_{x\rightarrow 0}\frac{1}{\frac{x}{\sin{x}}} &= \frac{1}{1}\\
\lim\limits_{x\rightarrow 0}\frac{\sin{x}}{x} &= 1\\
\end{align*}
$$

There we go! We have proven that $\lim\limits_{x\rightarrow 0}\frac{\sin{x}}{x} = 1$.

===

# Limits of Logarithmic-related Functions

<Formula>
$\lim\limits_{x\rightarrow \infty} \left ( 1 + \frac{1}{x} \right) ^ x = e$
</Formula>

The euler number is often defined as above. Using the above, we can quickly prove that, for a constant $k$, $\lim\limits_{x\rightarrow \infty} \left ( 1 + \frac{k}{x} \right) ^ x = e^k$.
Let us first define a new variable $y$, such that $y = \frac{x}{k}$.

$$
\begin{align*}
\lim\limits_{x\rightarrow \infty}\left ( 1 + \frac{k}{x} \right) ^ x
&= \lim\limits_{x\rightarrow \infty}\left ( 1 + \frac{1}{\frac{x}{k}} \right) ^ x \\
&= \lim\limits_{y\rightarrow \infty}\left ( 1 + \frac{1}{y} \right) ^ {yk} \\
&= \lim\limits_{y\rightarrow \infty}\left [ \left ( 1 + \frac{1}{y} \right) ^ y \right ] k \\
&= e^k \\
\end{align*}
$$

Since $x$ is approaching $\infty$, when we swap out the variables of the limit, $y$ is also approaching $\infty$ because $x$ and $y$ are proportional. Consequently, the above proof is possible.

---

<Formula>
Given the constant $a$, $\lim\limits\_{x\rightarrow 0} \frac{a^x - 1}{x} = \ln{a} $
</Formula>

To prove the above limit, we must first know that $a^x$ can be represented as a series, that is:  
$$1 + x \ln{a} + \frac{\left ( x \ln{a} \right )^2}{2!} + \frac{\left ( x \ln{a} \right )^3}{3!} + ...$$

$$
\begin{align*}
\lim\limits_{x\rightarrow 0} \frac{a^x - 1}{x}
&= \lim\limits_{x\rightarrow 0} \frac{\left[ 1 + x \ln{a} + \frac{\left ( x \ln{a} \right )^2}{2!} + \frac{\left ( x \ln{a} \right )^3}{3!} + ... \right] - 1}{x} \\
&= \lim\limits_{x\rightarrow 0} \left [ \frac{x \ln{a} + \frac{\left ( x \ln{a} \right )^2}{2!} + \frac{\left ( x \ln{a} \right )^3}{3!} + ...}{x} \right ] \\
&= \lim\limits_{x\rightarrow 0} \left [ \frac{x \ln{a} + \frac{x^2 \left ( \ln{a} \right )^2}{2!} + \frac{x^3 \left ( \ln{a} \right )^3}{3!} + ...}{x} \right ] \\
&= \lim\limits_{x\rightarrow 0} \left [ \ln{a} + \frac{x \left ( \ln{a} \right )^2}{2!} + \frac{x^2 \left ( \ln{a} \right )^3}{3!} + ... \right ] \\
&= \ln{a} \\
\end{align*}
$$

The remaining infinite terms would be zero due to $x$, so the only term that matters is the first one, that is $\ln{a}$, which would be the limit.

===

# Practice: Limits of Trigonometric and Logarithmic Functions

Match the limits on the left side, with the numbers on the right side.

`[match]@limit-trig-1@$\lim\limits_{x\rightarrow \infty} \frac{\csc^{-1}{(x)}}{x^2 - x}$@$0$@`
`[match]@limit-trig-2@$\lim\limits_{a\rightarrow \infty} \frac{\tan^{-1}{a}}{a}$@$1$@`
`[match]@limit-trig-3@$\lim\limits_{x\rightarrow 0} \frac{\left(1-\cos\left(4x\right)\right)}{2x\sin\left(x\right)} $@$4$@`
`[match]@limit-trig-4@$\lim\limits_{x\rightarrow 0} \frac{\sin{x}+\tan{x}}{\tan{2x}-\sin{x}}$@$2$@`
`[match]@limit-trig-5@$\lim\limits_{x\rightarrow 0} \frac{\cos{17x} - \cos{8x}}{\cos{7x} - \cos{2x}}$@$5$@`
`[match]@limit-log-1@$\lim\limits_{x\rightarrow 0} \ln{ \left [ (1 + 3x)^{\frac{1}{x}} \right ] }$@$3$@`
`[match]@limit-log-2@$\lim\limits_{x\rightarrow 0} \frac{\ln{\left ( \frac{1}{3} + x \right )} - \ln{\left ( \frac{1}{3} - x \right )}}{x}$@$6$@`
